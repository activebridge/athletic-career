.col-xs-12.col-sm-12.user-list
  .col-sm-2
    = image_tag(user.avatar)
  .col-sm-10.user-name
    h4
      = link_to user.name, user_path(user)
    table
      tr
        - user.distances.counter.each do |length, count|
          td.length-info
            .medal class=("length-#{length}").parameterize
            .count = count
