h1= t('.title')

table.table
  thead
    tr
      th= t('.name')
      th= t('.body')
      th= t('.image')
      th
      th
      th
  tbody
    = render partial: 'article', collection: articles, as: :article
br

= link_to :new_article, new_admin_article_path, class: 'btn btn-default'
